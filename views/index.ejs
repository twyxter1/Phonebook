
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://kit.fontawesome.com/697c2c5344.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>


    <div class="box">
        <% for(let i = 0; i < contacts.length; i++) { %>
            <form action="/delete" method="POST">
                <div class="item">
                    <input type="checkbox" value="<%= contacts[i].contact.name %>" name="checkbox" onchange="this.form.submit()" >
                    <p><%= contacts[i].contact.name %></p>  <p><%= contacts[i].contact.phone %></p>
                   
                </div>
            </form>
            
        <% } %>    

        

        <form action="/addContact" class="class" method="POST">
            <input type="text" name="nimi" placeholder="Lisa nimi">
            <input type="text" name="telefon" placeholder="Lisa number">
            <button type="submit">+</button>
        </form>
    </div>
    
    <footer>
        <span>Johannes</span>
    </footer>
</body>
</html>
